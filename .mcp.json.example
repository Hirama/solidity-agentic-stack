{
  "_comment": "MCP = the agent's hands. No official Foundry MCP exists, so this stack ships Foundry knowledge as skills under .claude/skills/ instead. Blockscout MCP covers chain reads. Copy to .mcp.json.",
  "mcpServers": {
    "blockscout": {
      "_comment": "Blockscout MCP — on-chain read access: address info, txs, ABIs, token transfers, contract storage. Read-only.",
      "url": "https://mcp.blockscout.com/mcp"
    }
  }
}
